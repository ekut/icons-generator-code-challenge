# Replicate API Token
# For development/testing: use "mock-token-for-development" (tests use mocks, no real API calls)
# For production: get your token from https://replicate.com/account/api-tokens
REPLICATE_API_TOKEN=mock-token-for-development

NODE_ENV=development
AWS_REGION=us-east-1
